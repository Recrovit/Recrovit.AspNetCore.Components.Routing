@inherits LayoutComponentBase

<AppShell SectionLabel="App layout / SSR + InteractiveServer"
          SectionSummary="This layout hosts SSR and InteractiveServer pages coming from the server project."
          VariantClass="app-shell--default">
    @Body
</AppShell>
