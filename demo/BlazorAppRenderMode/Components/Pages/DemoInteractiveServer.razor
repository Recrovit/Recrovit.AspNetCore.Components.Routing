@page "/demo-interactiveserver"
@attribute [RecrovitPageRoute(RecrovitRouteMode.InteractiveServer, LayoutType = typeof(AppLayout))]

<PageTitle>Interactive Server</PageTitle>

<RenderModeShowcase Title="Interactive Server"
                    Description="This sample page runs in top-level InteractiveServer mode from the app project, so it remains a server-side interactive section." />
