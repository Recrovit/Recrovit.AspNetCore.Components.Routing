@namespace BlazorAppRenderMode.Client
@page "/demo-interactiveauto"
@attribute [RecrovitPageRoute(RecrovitRouteMode.InteractiveAuto)]

<PageTitle>Interactive Auto</PageTitle>

<RenderModeShowcase Title="Interactive Auto"
                    Description="This sample page runs in top-level InteractiveAuto mode, so it uses the client project's layout and the active mode is shown dynamically." />
